# 下一步开发入口

已完成迭代：`ITER-00`至`ITER-51`。通知公告、站内消息、模板和账单冲销通知已落地，其余业务事件触发仍未完成；线上试色的分类、色号和多素材管理已落地，合同导入动作仍待格式确认。服务档案、库存、完整薪酬规则引擎和真实电子退款仍是明确的未完成项。

## 持续开发顺序

1. 以产品主档为基础实现库存余额、不可变出入库流水和盘点闭环；先核对旧系统礼品调拨、盘点及积分兑换结构，不虚构负库存和成本口径。
2. 甲方确认线上试色CSV/Excel字段、分类层级、图片包、覆盖策略和单批上限后，接异步导入、逐行结果和输入文件清理。
3. 按甲方字段确认补会员咨询卡、常用色号和服务档案；当前不虚构问卷字段。
4. 在独立储值/次卡冲销状态机落地时复用通知发布接口；预约、到期、生日、日报等任务须先确认触发时间、接收门店和去重周期。
5. 甲方确认服务反馈接收人、频率和升级层级后，基于通知底座补主动提醒；当前页面超时筛选和红色提示已完成。
6. 短信账号和上行回调就绪后，将已落地满意度规则接入手机号匹配和自动识别；当前试算不写业务数据。
7. 甲方确认六种模式名称、阶梯边界、跨档算法和计算周期后，接入累计阶梯并扩展多人技师/售卡分配。
8. 补充门店等级、岗位组合、店长提成和跨月负向调整规则及工资单。
9. 支付沙箱就绪后实现电子退款`PENDING/SUCCESS/FAILED`状态机、回调和对账；当前仅支持本地支付/退款事实联调。
10. Docker权限就绪后验证43个Migration、通知/审计/支付方式/服务小区/取消原因/首页图片/线上试色/分类/单位/职务权限、消息范围/阅读锁/业务事件唯一索引、操作日志分页SQL、各主数据rowversion、素材并发上限、任务租约竞争/强杀恢复、文件清理、会员/服务权限、归属到期任务、MinIO私有桶及核心事务回滚。

## 下次检查命令

```bash
cd /home/limeng/Documents/code/github/yuezhijian
git status --short
git log -3 --oneline
sed -n '1,280p' memory/DEVELOPMENT_STATUS.md
sed -n '1,200p' memory/NEXT_ACTIONS.md
```

若Memory与代码、测试或Git状态冲突，以仓库实际结果为准，并立即修正Memory。
