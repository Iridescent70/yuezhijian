# 下一步开发入口

已完成并提交迭代：`ITER-00`至`ITER-51`。`ITER-52`礼品库存、调拨和盘点已经完成编码与自动化测试，正在执行SQL Server真实落库验收，通过并提交后才能标记完成。线上试色合同导入、服务档案、积分兑换、完整薪酬规则引擎和真实电子退款仍是明确的未完成项。

## 持续开发顺序

1. 启动本地SQL Server，执行44个Flyway Migration；通过真实API新建会员后直查`mem_member`、会员卡、储值/积分账户，再重启后端复查数据仍存在。未通过前不允许使用memory运行结果作为完成证据。
2. 完成`ITER-52`礼品资料、调拨、盘点的SQL Server建单、余额、流水、状态机和重启持久化验证，更新验收记录后提交推送。
3. 基于礼品库存底座开发积分兑换：必须同一事务扣会员积分、扣门店库存、写积分流水和库存流水，并有幂等与冲销规则。
4. 甲方确认线上试色CSV/Excel字段、分类层级、图片包、覆盖策略和单批上限后，接异步导入、逐行结果和输入文件清理。
5. 按甲方字段确认补会员咨询卡、常用色号和服务档案；当前不虚构问卷字段。
6. 在独立储值/次卡冲销状态机落地时复用通知发布接口；预约、到期、生日、日报等任务须先确认触发时间、接收门店和去重周期。
7. 甲方确认服务反馈接收人、频率和升级层级后，基于通知底座补主动提醒；当前页面超时筛选和红色提示已完成。
8. 短信账号和上行回调就绪后，将已落地满意度规则接入手机号匹配和自动识别；当前试算不写业务数据。
9. 甲方确认六种模式名称、阶梯边界、跨档算法和计算周期后，接入累计阶梯并扩展多人技师/售卡分配。
10. 补充门店等级、岗位组合、店长提成和跨月负向调整规则及工资单；支付沙箱就绪后实现真实退款状态机、回调和对账。

## 下次检查命令

```bash
cd /home/limeng/Documents/code/github/yuezhijian
git status --short
git log -3 --oneline
sed -n '1,280p' memory/DEVELOPMENT_STATUS.md
sed -n '1,200p' memory/NEXT_ACTIONS.md
```

若Memory与代码、测试或Git状态冲突，以仓库实际结果为准，并立即修正Memory。
