# 下一步开发入口

已完成迭代：`ITER-00`至`ITER-48`。取消原因管理及预约/账单动态联动已经落地；职务、员工、工位、产品/服务分类和单位维护，服务项目和产品资料异步导入、产品单条维护、导出、批量上下架、对象级操作历史、系统操作日志、支付方式及服务小区管理也已完成。首页图片、服务档案、库存、完整薪酬规则引擎和真实电子退款仍是明确的未完成项。

## 持续开发顺序

1. 基于既有私有文件能力实现首页图片管理，覆盖图片文件、跳转地址、排序、展示位置、有效期、启停、rowversion、审计和前端预览；不加入未经确认的裁剪规则。
2. 按甲方字段确认补会员咨询卡、常用色号和服务档案；当前不虚构问卷字段。
3. 以产品主档为基础实现库存余额、不可变出入库流水和盘点闭环；先确认甲方负库存及成本口径。
4. 甲方确认服务反馈接收人、频率和升级层级后，基于通知任务补主动提醒；当前页面超时筛选和红色提示已完成。
5. 短信账号和上行回调就绪后，将已落地满意度规则接入手机号匹配和自动识别；当前试算不写业务数据。
6. 甲方确认六种模式名称、阶梯边界、跨档算法和计算周期后，接入累计阶梯并扩展多人技师/售卡分配。
7. 补充门店等级、岗位组合、店长提成和跨月负向调整规则及工资单。
8. 按甲方确认结果扩展券的门店、项目、分类、时段、叠加和批量客群发放规则；当前基础闭环已可运行。
9. 支付沙箱就绪后实现电子退款`PENDING/SUCCESS/FAILED`状态机、回调和对账；当前仅支持本地支付/退款事实联调。
10. Docker权限就绪后验证40个Migration、审计/支付方式/服务小区/取消原因/分类/单位/职务权限、操作日志分页SQL、支付门店配置/服务小区/取消原因/产品/员工/工位rowversion、过滤唯一索引、任务租约竞争/强杀恢复、导入输入清理、到期结果清理、会员/服务权限、归属到期任务、MinIO私有桶及核心事务回滚。

## 下次检查命令

```bash
cd /home/limeng/Documents/code/github/yuezhijian
git status --short
git log -3 --oneline
sed -n '1,280p' memory/DEVELOPMENT_STATUS.md
sed -n '1,200p' memory/NEXT_ACTIONS.md
```

若Memory与代码、测试或Git状态冲突，以仓库实际结果为准，并立即修正Memory。
