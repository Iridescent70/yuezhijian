# main 后端快照

`backend/` 是 `main@6cae2c8` 的自研 Spring Boot 后端完整快照，保留已有业务实现、测试和 44 个 Flyway 迁移，作为芋道重构的迁移基线。

- 本目录只用于阅读、比对和提炼回归测试，不再作为运行入口。
- 新业务代码进入仓库根目录 `backend/` 下的芋道单体工程。
- 迁移时应先确认业务口径，再按模块把规则、接口和测试移植到新工程，不能直接复制旧鉴权和基础设施代码。
- 数据迁移必须以甲方数据库备份、字段映射文档和可重复执行的迁移脚本为准。
