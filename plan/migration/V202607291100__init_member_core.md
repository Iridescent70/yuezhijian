# V202607291100 会员核心基线

| 项目 | 内容 |
| --- | --- |
| SQL 文件 | `V202607291100__init_member_core.sql` |
| 日期 | 2026-07-29 |
| 需求/缺陷 | API-MEM-001~003、UI-MEM-001~003 |
| 影响 API | 会员分页、建档、详情 |
| 影响页面 | 会员列表、新建会员、会员详情 |
| 影响表 | `mem_level`、`mem_member`、`mem_membership_card`、`mem_tag`、`mem_member_tag`、`ast_balance_account`、`ast_point_account` |
| 风险等级 | 中 |

## 变更原因

建立会员模块首个可运行闭环，并为后续储值、积分、次卡、预约和账单提供稳定的会员主键及当前资产账户。

## DDL/数据变化

新增7张表、会员手机号唯一哈希索引、门店/状态/姓名查询索引和有效标签唯一索引。初始化普通会员等级及3个基础标签，不写业务演示会员。

手机号只保存AES-GCM密文、带业务密钥的SHA-256检索哈希和尾号；Java日志及接口列表不返回完整手机号。建档时同时创建储值账户、积分账户和会员卡，整个操作在同一事务内完成。

## 兼容和数据回填

这是新增结构，不修改既有表。原计划中的 `V202607290930` 尚未落地，而仓库已经提交更高版本，因此本次只能追加 `V202607291100`，避免已执行环境需要开启Flyway乱序执行。

## 验证与预期

执行文件尾部查询：表数7、等级数1、标签数3；验证重复手机号哈希和重复会员卡号均被拒绝，建档失败时会员及两个资产账户全部回滚。

## 部署与失败恢复

在现有最高版本成功后执行。共享环境上线前备份，失败时停止应用并恢复备份；不得修改已执行SQL或使用 `flyway clean`。

## 各环境执行记录

| 环境 | 执行时间 | 执行人/流水线 | 耗时 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 本地 | 待 SQL Server 镜像就绪 | - | - | 待执行 | - |
