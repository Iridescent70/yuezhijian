# V202607290910 权限与组织基线

| 项目 | 内容 |
| --- | --- |
| SQL 文件 | `V202607290910__init_iam_org.sql` |
| 日期 | 2026-07-29 |
| 需求/缺陷 | ITER-00、系统管理/组织门店 |
| 影响 API | 登录、当前用户、菜单、门店、角色 API |
| 影响页面 | 登录、主框架、组织门店、角色管理 |
| 影响表 | `org_*`、`iam_*`、`SPRING_SESSION*` |
| 风险等级 | 中 |

## 变更原因

建立首个纵向样板需要的组织、门店、用户、角色、权限和菜单结构，并将生产会话交给 Spring Session JDBC 管理。

## DDL/数据变化

新建组织、门店、职务、员工、用户、角色、权限、角色关联、门店范围、菜单和两张 Spring Session 表。初始化总部/示范店、两个角色、10 个权限和 7 个菜单；不写默认用户和密码。

## 兼容和数据回填

全是新表。旧系统用户迁移时不得直接沿用弱密码散列，需生成首次登录重置流程。当前 Java memory profile 仍使用环境变量中的本地管理员，数据库认证适配放在下一迭代。

## 验证 SQL 与预期结果

执行文件尾部查询：权限 10、角色 2、菜单 7、门店 1；检查外键和唯一索引均启用，并验证 Spring Session 可创建、读取和过期删除会话。

## 部署与失败恢复

在 0900 成功后执行。部署前备份；失败时停止应用发布并恢复备份，不能修改已经在共享环境成功执行的 SQL。

## 各环境执行记录

| 环境 | 执行时间 | 执行人/流水线 | 耗时 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 本地 | 待 SQL Server 就绪 | - | - | 待执行 | - |
