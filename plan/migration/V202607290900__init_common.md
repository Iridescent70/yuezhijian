# V202607290900 公共能力基线

| 项目 | 内容 |
| --- | --- |
| SQL 文件 | `V202607290900__init_common.sql` |
| 日期 | 2026-07-29 |
| 需求/缺陷 | ITER-00、公共质量与交付 |
| 影响 API | API-AUTH/FILE/JOB/AUDIT 后续实现基础 |
| 影响页面 | 系统配置、下载中心、审计日志 |
| 影响表 | `sys_parameter`、`sys_dictionary`、`sys_file_object`、`sys_async_job`、`sys_idempotency_record`、`sys_audit_log` |
| 风险等级 | 低 |

## 变更原因

把配置、文件、批量任务、幂等和操作审计作为公共能力先落库，避免各业务模块重复建表。

## DDL/数据变化

新建 6 张表及按字典类型、文件所有者、任务状态、幂等过期时间、审计对象/用户/时间建立的索引。本版本不写入环境密钥或业务测试数据。

## 兼容和数据回填

全是新表，无旧数据回填。现有 memory profile 不依赖这些表；启用 sqlserver profile 后由 Flyway 自动创建。

## 验证 SQL 与预期结果

执行 SQL 文件尾部的 `sys.tables` 查询，`object_count` 应为 6；`dbo.flyway_schema_history` 中本版本应为成功。

## 部署与失败恢复

先备份目标库，再执行 `flyway validate` 和 `flyway migrate`。空库失败可修复 SQL 后重建；共享库失败必须保留现场并从备份恢复，禁止 `flyway clean`。

## 各环境执行记录

| 环境 | 执行时间 | 执行人/流水线 | 耗时 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 本地 | 待 SQL Server 就绪 | - | - | 待执行 | - |
