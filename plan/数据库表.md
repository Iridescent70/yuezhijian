# 悦指间系统初始数据库设计

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 日期 | 2026-07-29 |
| 数据库 | Microsoft SQL Server 2022 |
| 字符集/排序规则 | 中文环境使用 `Chinese_PRC_100_CI_AS_SC_UTF8`，部署前以原库实测结果确认 |
| 变更工具 | Flyway；所有 DDL、索引、基础数据和存储过程均进入 Migration |
| 依据 | 合同功能清单、旧系统 473 个 Hibernate 映射、API 和页面设计 |

旧系统已经确认使用 SQL Server，核心对象包括 `cst_customers`、`cst_membership_cards`、`cst_mc_accounts`、`cst_mc_item_suite_accounts`、`oe_bill_headers`、`oe_bill_lines` 等。新系统保留业务语义，不原样复制旧表：拆分会员、资产、账单、支付、提成和审计，去除大量 `text_attribute*` 扩展字段及映射中的公式查询。

## 1. 数据库和环境

| 数据库 | 用途 | 规则 |
| --- | --- | --- |
| `yuezhijian_legacy` | 恢复甲方旧库 | 迁移期只读，应用账号禁止写入 |
| `yuezhijian_dev` | 本地开发 | 可重建；必须通过 Flyway 创建 |
| `yuezhijian_test` | 自动化和联调 | 每次流水线从空库迁移并执行测试 |
| 预生产/生产库 | 演练和正式业务 | 禁止 `flyway clean`，上线前备份和恢复验证 |

首版使用 `dbo` schema 和模块前缀，避免一个月内引入过多 schema 权限复杂度。表前缀：`iam` 权限、`org` 组织、`cat` 商品、`mem` 会员、`ast` 资产、`apt` 预约、`trd` 交易、`inv` 库存、`vis` 回访、`mkt` 营销、`ntf` 通知、`comm` 提成、`payroll` 工资、`home` 到家、`ai` AI、`intg` 集成。

## 2. 字段和建模规则

### 2.1 类型规范

| 场景 | SQL Server 类型 | 规则 |
| --- | --- | --- |
| 主键/外键 | `bigint` | 主键默认 `identity(1,1)`；迁移不复用旧 id |
| 业务编号 | `varchar(32~64)` | 账单、预约、流水、批次等必须唯一 |
| 金额/比例 | `decimal(19,4)` | 禁止 `float/real/money`；界面通常展示 2 位 |
| 次数/积分 | `int` 或 `decimal(19,4)` | 可拆分次数时用 decimal，否则 int |
| 状态/类型 | `varchar(32)` | 代码用英文枚举，含义进入字典 |
| 中文短文本 | `nvarchar(50~500)` | 根据业务限定长度，禁止无目的 `nvarchar(max)` |
| JSON | `nvarchar(max)` | 加 `ISJSON(column)=1` 检查；只用于规则快照/第三方摘要 |
| 时间 | `datetime2(3)` | 保存 UTC；业务日期另用 `date` |
| 布尔 | `bit` | 默认值明确，不允许三态时 `not null` |
| 并发版本 | `rowversion` | 可修改聚合根必须带 `row_version` |
| 敏感值 | `varbinary`/密文 `nvarchar` | 手机、证件等使用应用层加密并另存哈希检索列 |

### 2.2 公共字段

除纯关联表和不可变流水外，可修改业务表默认包含：

```text
id bigint identity primary key
status varchar(32) not null
created_at datetime2(3) not null
created_by bigint null
updated_at datetime2(3) not null
updated_by bigint null
row_version rowversion not null
```

多门店业务表还必须包含 `biz_org_id bigint not null` 和 `store_id bigint null`。历史数据不得通过物理删除消失；基础资料使用 `status`/`disabled_at`，业务单据使用取消、作废和冲销状态。流水表为 append-only，只允许追加反向记录。

### 2.3 关键约束

- 会员手机号用 `mobile_ciphertext + mobile_hash + mobile_last4`；按手机号检索走带盐哈希，不保存明文日志。
- `trd_bill.total_receivable = sum(trd_bill_line.receivable_amount)`；结算时 `资产抵扣 + 外部实收 = 应付`，必须在同一事务校验。
- `ast_*_ledger`、`comm_ledger`、`inv_stock_ledger` 均包含 `before_balance/change_amount/after_balance` 并检查前后恒等式。
- 所有结算、充值、冲销、调账和第三方回调保存 `idempotency_key`，范围内建唯一索引。
- 报表不建立第二套可手改金额；从业务事实表计算，需要缓存时使用可重建快照。

## 3. 主要关系

```text
org_store ──< org_employee ──< apt_appointment_employee
    │              │                    │
    │              └──< trd_bill_line_employee >── trd_bill_line
    │                                                   │
mem_member ──< mem_membership_card                      ├── trd_bill_payment
    │                                                   ├── trd_adjustment
    ├── ast_balance_account ──< ast_balance_ledger      └── trd_reversal
    ├── ast_point_account ──< ast_point_ledger
    └── ast_member_card ──< ast_member_card_balance ──< ast_member_card_ledger

trd_bill/trd_bill_line ──> comm_ledger ──> payroll_employee_result
trd_bill/trd_bill_line ──> vis_visit_task / mem_service_record
```

## 4. 权限、组织和公共表

以下“字段”列只列业务字段；没有特别说明的可修改表均附带第 2.2 节公共字段。

| 表名 | 主要字段（类型） | 约束和用途 |
| --- | --- | --- |
| `iam_user` | `username varchar(64)`, `password_hash varchar(255)`, `full_name nvarchar(100)`, `employee_id bigint`, `is_admin bit`, `locked_at datetime2`, `last_login_at datetime2`, `password_changed_at datetime2` | `username` 唯一；不保存明文密码 |
| `iam_role` | `role_code varchar(64)`, `role_name nvarchar(100)`, `data_scope_type varchar(32)`, `description nvarchar(500)` | `role_code` 唯一；总部/门店/本人范围 |
| `iam_user_role` | `user_id bigint`, `role_id bigint`, `assigned_at datetime2`, `assigned_by bigint` | 联合主键 `(user_id, role_id)` |
| `iam_menu` | `parent_id bigint`, `menu_code varchar(64)`, `name nvarchar(100)`, `route varchar(255)`, `component varchar(255)`, `icon varchar(64)`, `sort_no int`, `client_type varchar(16)`, `permission_code varchar(128)` | 菜单树；code 唯一 |
| `iam_permission` | `permission_code varchar(128)`, `permission_name nvarchar(100)`, `resource_type varchar(32)`, `api_pattern varchar(255)`, `http_method varchar(16)` | 菜单/按钮/API/字段权限字典 |
| `iam_role_permission` | `role_id bigint`, `permission_id bigint`, `effect varchar(8)`, `field_mask_rule varchar(64)` | 联合唯一；effect 为 ALLOW/DENY |
| `iam_role_store_scope` | `role_id bigint`, `store_id bigint` | 自定义门店数据范围 |
| `iam_session_registry` | `session_id_hash char(64)`, `user_id bigint`, `client_type varchar(16)`, `device_id varchar(128)`, `ip varchar(64)`, `created_at/last_seen_at/expires_at datetime2`, `revoked_at datetime2`, `revoke_reason nvarchar(500)` | 会话审计和强制下线；不存原始会话 id；实际会话属性使用 Spring Session JDBC 表 |
| `spring_session`、`spring_session_attributes` | 由 Spring Session 官方 SQL 定义，至少含主 id、会话 id、创建/最后访问/过期时间及序列化属性；当前门店上下文作为会话属性保存 | 首版 Migration 纳入官方 SQL；门店切换不修改员工主门店，应用代码不得绕过框架直接改表 |
| `org_organization` | `parent_id bigint`, `org_code varchar(64)`, `org_name nvarchar(150)`, `org_type varchar(32)`, `path varchar(1000)`, `sort_no int` | 公司/区域/门店树；code 唯一 |
| `org_store` | `organization_id bigint`, `store_code varchar(64)`, `store_name nvarchar(150)`, `store_level varchar(32)`, `phone varchar(32)`, `province/city/district nvarchar(64)`, `address nvarchar(300)`, `longitude/latitude decimal(10,7)`, `business_hours_json nvarchar(max)` | organization 一对一；地址/营业时间 |
| `org_position` | `position_code varchar(64)`, `position_name nvarchar(100)`, `position_level int`, `default_service_rate/default_sales_rate decimal(9,6)` | 职务和默认比例 |
| `org_employee` | `employee_no varchar(64)`, `name nvarchar(100)`, `mobile_ciphertext nvarchar(500)`, `mobile_hash char(64)`, `position_id bigint`, `primary_store_id bigint`, `hire_date date`, `leave_date date`, `can_service bit`, `can_sell bit`, `status`, `row_version` | 员工号唯一；手机号索引哈希；资料更新使用乐观锁，手机号不回显原文 |
| `org_employee_store` | `employee_id bigint`, `store_id bigint`, `relation_type varchar(32)`, `effective_from/to date`, `allocation_rate decimal(9,6)` | 主店/兼职/支援历史，期间不得异常重叠 |
| `org_employee_loan` | `loan_no varchar(32)`, `employee_id bigint`, `source_store_id bigint`, `target_store_id bigint`, `start_date/end_date date`, `cash_rate/consumption_rate/traffic_rate decimal(9,6)`, `reason nvarchar(500)`, `approval_status varchar(32)` | 借调申请和分配规则 |
| `org_workstation` | `store_id bigint`, `workstation_code varchar(64)`, `name nvarchar(100)`, `capacity int`, `sort_no int`, `status`, `row_version` | 门店内 code 唯一；资料更新使用乐观锁；编号和所属门店创建后不可修改 |
| `org_terminal` | `store_id bigint`, `terminal_code varchar(64)`, `name nvarchar(100)`, `device_fingerprint varchar(255)`, `last_seen_at datetime2` | 终端绑定，不保存硬件敏感信息原文 |
| `sys_parameter` | `param_group varchar(64)`, `param_key varchar(128)`, `value_ciphertext nvarchar(max)`, `value_type varchar(32)`, `is_secret bit`, `description nvarchar(500)` | `(group,key)` 唯一；非密钥参数维护已落地，密钥不查询/不回显；回访/反馈时限键为`VISIT/AFTER_SALE_DUE_HOURS`、`VISIT/SERVICE_FEEDBACK_DUE_HOURS` |
| `sys_dictionary` | `dict_type varchar(64)`, `item_code varchar(64)`, `item_name nvarchar(100)`, `item_value nvarchar(500)`, `sort_no int` | 业务枚举展示；不可替代强状态机 |
| `sys_cancel_reason` | `business_type varchar(32)`, `reason_code varchar(64)`, `reason_name nvarchar(200)`, `requires_note bit`, `sort_no int` | 预约/账单/到家取消原因 |
| `sys_audit_log` | `trace_id varchar(64)`, `user_id bigint`, `store_id bigint`, `module varchar(64)`, `action varchar(64)`, `object_type varchar(64)`, `object_id varchar(64)`, `before_json/after_json nvarchar(max)`, `result varchar(16)`, `error_code varchar(32)`, `ip varchar(64)`, `occurred_at datetime2` | 按日期、对象、用户索引；敏感值脱敏 |
| `sys_file_object` | `object_key varchar(500)`, `original_name nvarchar(255)`, `content_type varchar(128)`, `size_bytes bigint`, `sha256 char(64)`, `purpose varchar(64)`, `owner_user_id bigint`, `expires_at datetime2`, `access_level/status varchar(32)` | 已接入；OSS/MinIO私有元数据，对象key唯一且不通过业务API返回；任务输入使用`ASYNC_JOB_INPUT`并在终态清理；状态`ACTIVE/DELETED/QUARANTINED` |
| `sys_file_attachment` | `file_id`, `business_type varchar(64)`, `business_id bigint`, `store_id bigint`, `category varchar(64)`, `created_at/by`, `removed_at/by` | 已接入；通用业务附件绑定，文件只能绑定一次，下载先校验业务与权限，删除保留历史 |
| `sys_async_job` | `job_no varchar(32)`, `job_name nvarchar(200)`, `job_type varchar(64)`, `request_json nvarchar(max)`, `store_id/created_by bigint`, `input_file_id/result_file_id/error_file_id bigint`, `progress int`, `success_count/failure_count int`, `started_at/finished_at/expires_at/result_purged_at datetime2`, `lease_token varchar(36)`, `lease_expires_at datetime2`, `attempt_count int`, `error_message nvarchar(1000)` | 已接入；任务号唯一，按创建人查询/下载；输入文件ID不对前端返回并在任务终态清理；SQL跳锁及租约防止重复执行，结果到期后物理删除对象并保留审计元数据 |
| `sys_idempotency_record` | `scope varchar(64)`, `idempotency_key varchar(128)`, `request_hash char(64)`, `resource_type varchar(64)`, `resource_id varchar(64)`, `response_code varchar(32)`, `expires_at datetime2` | `(scope,key)` 唯一；重复请求返回同结果 |

## 5. 商品、服务、次卡和配置表

| 表名 | 主要字段（类型） | 约束和用途 |
| --- | --- | --- |
| `cat_category` | `parent_id bigint`, `category_type varchar(32)`, `category_code varchar(64)`, `name nvarchar(100)`, `path varchar(1000)`, `sort_no int` | 产品/服务/次卡/礼品分类树 |
| `cat_unit` | `unit_code varchar(32)`, `unit_name nvarchar(50)`, `decimal_places tinyint` | 单位 code 唯一 |
| `cat_product` | `product_code varchar(64)`, `product_name nvarchar(200)`, `category_id bigint`, `unit_id bigint`, `barcode varchar(64)`, `cost_price/sale_price decimal(19,4)`, `track_stock bit`, `description nvarchar(1000)`, `status`, `row_version` | 已接入；编码唯一、非空条码过滤唯一；历史账单仍须保留名称/价格快照 |
| `cat_service` | `service_code varchar(64)`, `service_name nvarchar(200)`, `category_id bigint`, `duration_minutes int`, `cost_amount/list_price decimal(19,4)`, `description nvarchar(2000)`, `instructions nvarchar(max)` | 服务编码唯一 |
| `cat_service_material` | `service_id bigint`, `product_id bigint`, `quantity decimal(19,4)`, `unit_id bigint`, `wastage_rate decimal(9,6)` | 服务物料成本；联合唯一 |
| `cat_item_store` | `item_type varchar(16)`, `item_id bigint`, `store_id bigint`, `sale_price decimal(19,4)`, `sort_no int`, `sale_status varchar(32)`, `effective_from/to datetime2` | 产品/服务门店配置 |
| `cat_tag` | `tag_type varchar(32)`, `tag_code varchar(64)`, `tag_name nvarchar(100)`, `color varchar(16)` | 商品及服务标签 |
| `cat_item_tag` | `item_type varchar(16)`, `item_id bigint`, `tag_id bigint` | 联合唯一 |
| `cat_card_type` | `card_type_code varchar(64)`, `card_type_name nvarchar(200)`, `category_id bigint`, `sale_price/list_price decimal(19,4)`, `total_times decimal(19,4)`, `valid_days int`, `purchase_threshold decimal(19,4)`, `instructions nvarchar(max)`, `auto_remind_days int` | 次卡定义；code 唯一 |
| `cat_card_type_store` | `card_type_id bigint`, `store_id bigint`, `sale_status varchar(32)`, `sort_no int` | 次卡适用门店 |
| `cat_card_service_rule` | `card_type_id bigint`, `service_id bigint`, `included_times/deduct_times decimal(19,4)`, `priority int`, `effective_from/to datetime2` | 项目-次卡多对多匹配及售卡初始次数 |
| `cat_card_commission_rule` | `card_type_id bigint`, `rule_scene varchar(32)`, `mode varchar(32)`, `position_id bigint`, `fixed_amount decimal(19,4)`, `rate decimal(9,6)`, `include_card_performance bit`, `rule_json nvarchar(max)` | 售卡/消耗两套提成规则 |
| `cat_payment_method` | `method_code varchar(64)`, `method_name nvarchar(100)`, `method_type varchar(32)`, `is_electronic bit`, `included_in_revenue bit`, `needs_external_ref bit` | 支付方式 code 唯一 |
| `cat_payment_method_store` | `payment_method_id bigint`, `store_id bigint`, `sort_no int`, `enabled bit` | 门店排序和启用状态 |
| `cat_voucher` | `voucher_code varchar(64)`, `voucher_name nvarchar(200)`, `benefit_type varchar(32)`, `face_amount decimal(19,4)`, `discount_rate decimal(9,6)`, `min_spend decimal(19,4)`, `valid_days int`, `commission_rule nvarchar(1000)`, `status`, `row_version` | 已由V1220落地；金额券/折扣券定义，编码唯一并做金额组合约束 |
| `ben_voucher_issue_batch` | `batch_no varchar(32)`, `voucher_id/member_id bigint`, `issue_count int`, `idempotency_key varchar(128)`, `created_by/at` | 已由V1220落地；一次生成1~100张，幂等键唯一 |
| `ben_voucher_code` | `code varchar(64)`, `issue_batch_id/voucher_id/member_id bigint`, 定义快照字段, `valid_from/until`, `status`, `bound_at/redeemed_at`, `redeemed_bill_id`, `row_version` | 已由V1220落地；`UNBOUND/BOUND/REDEEMED/EXPIRED/VOIDED`状态，不回读模板计算历史券 |
| `ben_voucher_ledger` | `ledger_no`, `code_id`, `ledger_type`, `member_id`, `amount`, `source_bill_id/source_reversal_id`, `reversed_ledger_id`, `idempotency_key` | 已由V1220落地；绑定、核销、返券不可变流水，返券只能反向一次 |
| `cat_gift` | `gift_code varchar(64)`, `gift_name nvarchar(200)`, `category_id bigint`, `unit_id bigint`, `point_price int`, `cost_price decimal(19,4)`, `image_file_id bigint` | 积分礼品定义 |
| `cat_color_style` | `category_id bigint`, `color_code varchar(64)`, `color_name nvarchar(100)`, `image_file_id bigint`, `sort_no int` | 在线试色 |
| `cfg_banner` | `position_code varchar(64)`, `title nvarchar(200)`, `image_file_id bigint`, `link_type varchar(32)`, `link_value nvarchar(500)`, `sort_no int`, `valid_from/to datetime2` | PC/到家首页图片 |
| `cfg_service_area` | `store_id bigint`, `city/district nvarchar(64)`, `address nvarchar(300)`, `longitude/latitude decimal(10,7)`, `radius_km decimal(9,3)`, `visit_fee decimal(19,4)`, `operator_id bigint` | 到家服务范围；地图坐标索引按需要增加 |
| `cfg_receipt_template` | `template_code varchar(64)`, `template_name nvarchar(100)`, `store_id bigint`, `is_default bit`, `components_json nvarchar(max)`, `paper_width_mm int` | 组件和变量 JSON；门店最多一个默认模板 |

## 6. 会员、档案、标签和资产表

| 表名 | 主要字段（类型） | 约束和用途 |
| --- | --- | --- |
| `mem_member` | `member_no varchar(32)`, `full_name nvarchar(100)`, `nickname nvarchar(100)`, `gender varchar(16)`, `birthday date`, `mobile_ciphertext nvarchar(500)`, `mobile_hash char(64)`, `mobile_last4 char(4)`, `email nvarchar(255)`, `source_type varchar(32)`, `join_store_id bigint`, `owner_store_id bigint`, `advisor_employee_id bigint`, `level_id bigint`, `frozen_at datetime2`, `freeze_reason nvarchar(500)`, `special_flag bit`, `last_visit_at datetime2`, `status varchar(32)` | member_no、mobile_hash 唯一；手机号不存明文；门店+状态索引 |
| `mem_member_status_log` | `member_id bigint`, `from_status/to_status varchar(32)`, `reason nvarchar(500)`, `changed_at datetime2`, `changed_by bigint` | 已落地；冻结、解冻、停用等状态变化append-only，原因必填且前后状态不能相同 |
| `mem_member_advisor_log` | `member_id`, `owner_store_id`, `old_advisor_employee_id`, `new_advisor_employee_id`, `change_source`, `changed_at/by` | 已落地；手工/批量顾问变更append-only，允许分配或清空并保存前后值；按会员、按新顾问建立时间倒序索引 |
| `mem_membership_card` | `member_id bigint`, `card_no varchar(64)`, `password_hash varchar(255)`, `register_store_id bigint`, `register_user_id bigint`, `registered_at datetime2`, `revoke_store_id/user_id bigint`, `revoked_at datetime2`, `revoke_reason nvarchar(500)`, `previous_card_id bigint`, `status varchar(32)` | card_no 唯一；一会员可保留换卡历史 |
| `mem_level` | `level_code varchar(64)`, `level_name nvarchar(100)`, `stored_value_threshold decimal(19,4)`, `auto_upgrade bit`, `birthday_discount_rate decimal(9,6)`, `service_discount_rate decimal(9,6)`, `status varchar(32)` | 级别 code 唯一 |
| `mem_tag` | `tag_code varchar(64)`, `tag_name nvarchar(100)`, `tag_source varchar(16)`, `rule_json nvarchar(max)`, `color varchar(16)`, `negative_flag bit` | MANUAL/RULE/AI；code 唯一 |
| `mem_member_tag` | `member_id bigint`, `tag_id bigint`, `source varchar(16)`, `confidence decimal(5,4)`, `evidence_json nvarchar(max)`, `assigned_at datetime2`, `assigned_by bigint`, `removed_at datetime2` | 保留标签历史；有效标签过滤 removed_at |
| `mem_segment` | `segment_name nvarchar(100)`, `owner_user_id bigint`, `filter_json nvarchar(max)`, `shared_scope varchar(32)`, `last_count int` | 保存常用会员筛选 |
| `mem_note` | `member_id bigint`, `note_type varchar(32)`, `content nvarchar(2000)`, `negative_flag bit`, `file_ids_json nvarchar(max)`, `created_at datetime2`, `created_by bigint` | append-only；更正生成新记录 |
| `mem_service_profile` | `member_id bigint`, `preferences nvarchar(2000)`, `contraindications nvarchar(2000)`, `favorite_colors nvarchar(1000)`, `structured_json nvarchar(max)` | 一会员一当前档案；历史入 revision |
| `mem_consultation_card` | `member_id bigint`, `form_schema_version varchar(32)`, `answers_json nvarchar(max)`, `signed_file_id bigint`, `completed_at datetime2` | 结构化咨询卡 |
| `mem_profile_revision` | `member_id bigint`, `profile_type varchar(32)`, `revision_no int`, `snapshot_json nvarchar(max)`, `change_reason nvarchar(500)`, `created_at datetime2`, `created_by bigint` | 服务档案/咨询卡历史 |
| `mem_ownership_adjustment` | `adjustment_no`, `member_id`, `old_store_id/new_store_id`, `effective_date`, `share_rule_json`, `reason`, `approval_status`, `execution_status`, 申请/审批/执行字段、`active_member_key`计算列和`row_version` | 已落地；审批和生效分离；活动记录映射会员id、结束记录映射负申请id并建唯一索引，保证同一会员最多一张WAITING/PROCESSING申请；历史业务不改写 |
| `mem_consumption_relation` | `bill_id bigint`, `primary_member_id bigint`, `consumer_type varchar(32)`, `related_member_id bigint`, `consumer_name nvarchar(100)` | 本人/朋友/父母/子女/亲属/其他 |
| `ast_balance_account` | `member_id bigint`, `available_balance/frozen_balance/total_recharged decimal(19,4)`, `last_transaction_at datetime2` | member_id 唯一；余额聚合 |
| `ast_balance_ledger` | `ledger_no varchar(32)`, `account_id bigint`, `transaction_type varchar(32)`, `before_balance/change_amount/after_balance decimal(19,4)`, `source_type varchar(32)`, `source_id/source_line_id bigint`, `store_id bigint`, `occurred_at datetime2`, `correlation_id varchar(64)`, `reversed_ledger_id bigint` | append-only；ledger_no 唯一 |
| `ast_point_account` | `member_id bigint`, `available_points int`, `lifetime_points int`, `last_transaction_at datetime2` | member_id 唯一 |
| `ast_point_ledger` | `ledger_no varchar(32)`, `account_id bigint`, `transaction_type varchar(32)`, `before_points/change_points/after_points int`, `source_type varchar(32)`, `source_id bigint`, `expired_at datetime2`, `occurred_at datetime2`, `correlation_id varchar(64)`, `reversed_ledger_id bigint` | append-only；积分清零可追溯 |
| `ast_recharge_order` | `recharge_no varchar(32)`, `member_id bigint`, `account_id bigint`, `store_id bigint`, `recharge_amount/gift_amount decimal(19,4)`, `sales_employee_id bigint`, `confirmed_by/confirmed_at`, `cancel_reason nvarchar(500)`, `idempotency_key varchar(128)` | 充值确认后写余额流水 |
| `ast_member_card` | `card_no varchar(32)`, `member_id bigint`, `card_type_id bigint`, `source_order_id bigint NULL`, `purchase_store_id bigint`, `sale_employee_id bigint`, `purchase_price decimal(19,4)`, `started_at/expires_at datetime2`, `original_card_id bigint`, `transfer_from_card_id bigint` | 会员次卡实例；card_no 唯一；换卡生成的新卡无售卡单，通过original_card_id追溯原卡 |
| `ast_card_sale_order` | `order_no varchar(32)`, `member_id/card_type_id/store_id bigint`, `quantity int`, `unit_price/total_amount decimal(19,4)`, `payment_method_id bigint`, `external_reference varchar(128)`, `idempotency_key varchar(128)` | 售卡业务源单；幂等键唯一 |
| `ast_member_card_balance` | `member_card_id bigint`, `service_id bigint`, `total_times/remaining_times/frozen_times/deduct_times decimal(19,4)` | `(member_card_id,service_id)` 唯一；保留售卡时扣次规则快照 |
| `ast_member_card_ledger` | `ledger_no varchar(32)`, `member_card_id bigint`, `service_id bigint`, `transaction_type varchar(32)`, `before_times/change_times/after_times decimal(19,4)`, `value_amount decimal(19,4)`, `source_type varchar(32)`, `source_id/source_line_id bigint`, `occurred_at datetime2`, `correlation_id varchar(64)`, `reversed_ledger_id bigint` | 扣次/补次/换卡/转赠/退卡不可变流水 |
| `ast_card_exchange_quote` | `quote_no varchar(32)`, `old_card_id/target_card_type_id bigint`, `old_remaining_times/old_remaining_value/new_card_value/difference_amount decimal(19,4)`, `old_card_row_version/target_card_type_row_version varbinary(8)`, `expires_at/used_at datetime2` | 10分钟换卡报价；同时锁定原卡和目标卡种版本，报价一次性使用 |
| `ast_card_exchange` | `exchange_no varchar(32)`, `quote_id bigint`, `old_card_id/new_card_id bigint`, `member_id bigint`, `old_remaining_value/new_card_value/difference_amount decimal(19,4)`, `handled_store_id/handled_employee_id bigint`, `idempotency_key varchar(128)`, `executed_at datetime2` | 换卡事实；报价、原卡、新卡和幂等键均唯一 |
| `ast_card_exchange_payment` | `exchange_id/payment_method_id bigint`, `amount decimal(19,4)`, `external_reference varchar(128)`, `sort_no int` | 补差支付事实；同一换卡单支付方式不重复，合计由服务层强制等于补差 |
| `ast_card_transfer` | `transfer_no varchar(32)`, `source_card_id/target_card_id bigint`, `source_member_id/recipient_member_id bigint`, `remaining_times/remaining_value decimal(19,4)`, `old_expires_at/new_expires_at datetime2`, `handled_store_id/handled_employee_id bigint`, `reason nvarchar(500)`, `idempotency_key varchar(128)`, `executed_at datetime2` | 双方资产变动关联；源卡、目标卡和幂等键唯一 |
| `ast_card_refund_quote` | `quote_no varchar(32)`, `member_card_id bigint`, `original_amount/consumed_reprice_amount/fee_amount/refund_amount decimal(19,4)`, `card_row_version varbinary(8)`, `expires_at/used_at datetime2` | 10分钟退卡试算，锁定次卡版本和退款公式 |
| `ast_card_refund_quote_item` | `quote_id/card_ledger_id/bill_id/service_id bigint`, `bill_no_snapshot varchar(32)`, `service_name_snapshot nvarchar(200)`, `consumed_at datetime2`, `original_amount decimal(19,4)` | 每笔有效次卡消费对应的账单项目原价快照；已冲销消费不计入 |
| `ast_card_refund_request` | `request_no varchar(32)`, `quote_id/member_card_id/member_id bigint`, `original_amount/consumed_reprice_amount/refund_amount/fee_amount decimal(19,4)`, `refund_method_id bigint`, `handled_store_id/handled_employee_id bigint`, `status/commission_adjustment_status varchar(32)`, `card_row_version varbinary(8)`, `request/execution_idempotency_key varchar(128)`, 审批与执行字段 | 提交时冻结次卡；驳回恢复，执行后清零；提成冲回未完成时明确为PENDING_MODULE |
| `ast_card_refund_payment` | `refund_no varchar(32)`, `request_id/payment_method_id bigint`, `refund_amount decimal(19,4)`, `refund_status varchar(32)`, `external_refund_reference/idempotency_key varchar(128)` | 退卡退款事实；当前本地成功记录，真实电子退款通道后续接入 |

## 7. 预约、账单、支付、调账和冲销表

| 表名 | 主要字段（类型） | 约束和用途 |
| --- | --- | --- |
| `apt_employee_schedule` | `employee_id bigint`, `store_id bigint`, `work_date date`, `start_time/end_time time`, `schedule_type varchar(32)`, `capacity int`, `note nvarchar(500)` | 员工日排班；校验重叠 |
| `apt_appointment` | `appointment_no varchar(32)`, `member_id bigint`, `guest_name nvarchar(100)`, `mobile_ciphertext/hash/last4`, `store_id bigint`, `source_type varchar(32)`, `appointment_type varchar(32)`, `start_at/end_at datetime2`, `person_count int`, `workstation_id bigint`, `note nvarchar(1000)`, `arrived_at/started_at/completed_at/cancelled_at datetime2`, `cancel_reason_id bigint`, `bill_id bigint`, `idempotency_key varchar(128)` | 编号唯一；状态机控制 |
| `apt_appointment_service` | `appointment_id bigint`, `service_id bigint`, `service_name_snapshot nvarchar(200)`, `duration_minutes int`, `price_snapshot decimal(19,4)`, `sort_no int` | 预约项目快照 |
| `apt_appointment_employee` | `appointment_id bigint`, `employee_id bigint`, `role_type varchar(32)`, `start_at/end_at datetime2`, `is_designated bit` | 技师占用时段；防冲突索引/校验 |
| `apt_waitlist` | `member_id bigint`, `store_id bigint`, `service_id bigint`, `preferred_employee_id bigint`, `preferred_date date`, `time_range_start/end time`, `priority int`, `converted_appointment_id bigint`, `expired_at datetime2` | 候补队列 |
| `apt_status_history` | `appointment_id bigint`, `from_status/to_status varchar(32)`, `reason_code varchar(64)`, `note nvarchar(500)`, `occurred_at datetime2`, `operator_id bigint` | append-only 状态时间线 |
| `trd_bill` | `bill_no varchar(32)`, `appointment_id bigint`, `member_id bigint`, `store_id bigint`, `source_type varchar(32)`, `person_count int`, `currency char(3)`, `original_amount/discount_amount/receivable_amount/received_amount/change_amount decimal(19,4)`, `settled_at datetime2`, `cashier_id bigint`, `voided_at/by`, `void_reason_id bigint`, `correlation_id varchar(64)`, `idempotency_key/settlement_idempotency_key varchar(128)` | 编号唯一；创建和结算幂等键分别唯一 |
| `trd_bill_line` | `bill_id bigint`, `line_no int`, `item_type varchar(16)`, `item_id bigint`, `item_code/name_snapshot`, `unit_price/discount_amount/receivable_amount/actual_amount decimal(19,4)`, `quantity decimal(19,4)`, `commission_base decimal(19,4)`, `line_status varchar(16)`, `removed_at/by`, `note nvarchar(500)` | `(bill_id,line_no)` 唯一；删除使用`REMOVED`软删除，保留商品快照和历史外键 |
| `trd_bill_line_employee` | `bill_line_id bigint`, `employee_id bigint`, `employee_role varchar(32)`, `performance_store_id bigint`, `allocation_rate decimal(9,6)`, `performance_amount decimal(19,4)` | 多项目多技师分配；比例合计按规则校验 |
| `trd_bill_discount` | `batch_no varchar(32)`, `bill_id/bill_line_id bigint`, `discount_type varchar(32)`, `discount_value decimal(19,6)`, `original_amount/discount_amount decimal(19,4)`, `reason nvarchar(500)`, `authorization_user_id bigint`, `rule_snapshot_json nvarchar(max)`, `active bit`, `superseded_at datetime2` | 优惠逐行分摊和授权记录；每行最多一条有效优惠，重算时旧批次失效不删除 |
| `trd_settlement_quote` | `quote_no varchar(32)`, `bill_id bigint`, `bill_row_version varbinary(8)`, `receivable_amount/payment_total/asset_amount/external_payment_amount/change_amount/difference_amount decimal(19,4)`, `expires_at/used_at datetime2` | 10分钟试算快照；一次使用 |
| `trd_settlement_quote_asset` | `quote_id bigint`, `asset_type varchar(16)`, `member_id/voucher_code_id/member_card_id/member_card_balance_id/bill_line_id/service_id bigint`, `quantity/amount decimal(19,4)`, `asset_version varchar(128)`, `display_name nvarchar(200)` | 保存储值、积分、次卡、代金券选择及试算时版本；类型与引用组合受检查约束 |
| `trd_bill_asset_usage` | `bill_id bigint`, `asset_type varchar(16)`, `member_id/voucher_code_id/member_card_id/member_card_balance_id/bill_line_id/service_id bigint`, `quantity/amount decimal(19,4)`, `asset_ledger_id bigint`, `display_name nvarchar(200)` | 结算资产事实，可追溯到不可变资产流水；V1220扩展`VOUCHER` |
| `trd_payment` | `payment_no varchar(32)`, `bill_id bigint`, `payment_method_id bigint`, `amount decimal(19,4)`, `payment_status varchar(32)`, `external_order_no varchar(128)`, `authorization_no varchar(128)`, `paid_at datetime2`, `idempotency_key varchar(128)` | 编号、通道外部单号按通道唯一 |
| `trd_payment_refund` | `refund_no varchar(32)`, `reversal_id/payment_id bigint`, `refund_amount decimal(19,4)`, `refund_status varchar(32)`, `external_refund_no varchar(128)`, `requested_at/completed_at datetime2`, `idempotency_key varchar(128)`, `created_by bigint` | 支付退款事实；冲销+支付、退款号和幂等键唯一 |
| `trd_adjustment` | `adjustment_no varchar(32)`, `bill_id bigint`, `adjustment_type varchar(32)`, `reason nvarchar(1000)`, `before_snapshot/after_snapshot/impact_snapshot nvarchar(max)`, `approval_status varchar(32)`, `requested_by/at`, `approved_by/at`, `executed_at datetime2`, `correlation_id varchar(64)`, `idempotency_key varchar(128)` | 调账申请；不覆盖原账单 |
| `trd_reversal` | `reversal_no varchar(32)`, `bill_id bigint`, `refund_amount decimal(19,4)`, `reason nvarchar(1000)`, `status varchar(32)`, `request/execution_idempotency_key varchar(128)`, `requested/reviewed/executed_at/by`, `review_comment nvarchar(1000)`, `active_bill_id`计算列 | 首版整单冲销主单；同一账单只允许一个待处理或已执行冲销，状态`SUBMITTED/APPROVED/REJECTED/EXECUTED` |
| `trd_approval_step` | `business_type varchar(32)`, `business_id bigint`, `step_no int`, `approver_role_id/user_id bigint`, `decision varchar(32)`, `comment nvarchar(1000)`, `decided_at datetime2` | 调账、冲销、退卡等多级审批 |
| `trd_recommendation_card_record` | `bill_id/bill_line_id bigint`, `store_id bigint`, `employee_id bigint`, `card_type_id bigint`, `card_count int`, `amount decimal(19,4)`, `occurred_at datetime2` | 推荐卡业绩事实 |

## 8. 券码、库存、设备、回访和营销表

| 表名 | 主要字段（类型） | 约束和用途 |
| --- | --- | --- |
| `ben_member_voucher` | `account_no varchar(32)`, `voucher_id bigint`, `member_id bigint`, `code varchar(64)`, `face_amount/balance_amount decimal(19,4)`, `issued_at/expires_at/redeemed_at datetime2`, `source_type/source_id`, `redeem_bill_id bigint` | code 唯一；发放和核销状态 |
| `ben_service_code_job` | `job_no varchar(32)`, `category varchar(32)`, `quantity int`, `valid_from/to datetime2`, `channel varchar(32)`, `commission_rule_json nvarchar(max)`, `generated_count int`, `file_id bigint` | 批量生成任务 |
| `ben_service_code` | `code_hash char(64)`, `code_masked varchar(64)`, `job_id bigint`, `service_id bigint`, `member_id bigint`, `bound_at datetime2`, `redeemed_store_id/bill_id bigint`, `redeemed_at datetime2`, `expires_at datetime2` | 码只在授权场景明文展示；hash 唯一 |
| `ben_point_redemption` | `redemption_no varchar(32)`, `member_id bigint`, `store_id bigint`, `total_points int`, `operator_id bigint`, `reversed_at/by`, `reverse_reason nvarchar(500)` | 兑换主单 |
| `ben_point_redemption_line` | `redemption_id bigint`, `gift_id bigint`, `quantity int`, `unit_points/total_points int`, `stock_ledger_id bigint` | 礼品明细 |
| `inv_stock` | `store_id bigint`, `item_type varchar(16)`, `item_id bigint`, `on_hand_qty/reserved_qty/available_qty decimal(19,4)` | `(store,item_type,item)` 唯一 |
| `inv_stock_ledger` | `ledger_no varchar(32)`, `store_id bigint`, `item_type varchar(16)`, `item_id bigint`, `transaction_type varchar(32)`, `before_qty/change_qty/after_qty decimal(19,4)`, `source_type/source_id/source_line_id`, `occurred_at datetime2`, `reversed_ledger_id bigint` | append-only |
| `inv_transfer` | `transfer_no varchar(32)`, `source_store_id/target_store_id bigint`, `handled_by bigint`, `confirmed_at`, `voided_at`, `reversed_at`, `reason nvarchar(500)` | 调拨主单 |
| `inv_transfer_line` | `transfer_id bigint`, `gift_id bigint`, `quantity decimal(19,4)`, `source_ledger_id/target_ledger_id bigint` | 调拨明细 |
| `inv_count` | `count_no varchar(32)`, `store_id bigint`, `scope_json nvarchar(max)`, `counted_by bigint`, `counted_at/confirmed_at datetime2`, `difference_count int` | 盘点主单 |
| `inv_count_line` | `count_id bigint`, `item_type varchar(16)`, `item_id bigint`, `book_qty/actual_qty/difference_qty decimal(19,4)`, `adjustment_ledger_id bigint` | 差异明细 |
| `eqp_equipment` | `equipment_no varchar(64)`, `name nvarchar(200)`, `model nvarchar(100)`, `store_id bigint`, `responsible_employee_id bigint`, `purchase_date date`, `equipment_status varchar(32)` | 设备编号唯一 |
| `eqp_receipt` | `equipment_id bigint`, `receiver_employee_id bigint`, `receiver_store_id bigint`, `received_at datetime2`, `returned_at datetime2`, `condition_on_receive/return nvarchar(500)` | 领取归还历史 |
| `vis_visit_task` | `task_no varchar(32)`, `member_id/bill_id/store_id bigint`, `due_at datetime2(3)`, `task_type/status`, `complaint_flag bit`, `conclusion nvarchar(1000)`, `completed_at/canceled_at`, `cancel_reason`, 审计字段和`row_version` | 已落地；一张会员账单唯一一张任务，逾期由`PENDING+due_at`动态计算，冲销保留取消原因 |
| `vis_visit_participant` | `task_id bigint`, `employee_id bigint null`, `employee_name_snapshot`, `service_summary`, `status`, `completed_at`, 审计字段 | 已落地；一位技师一个参与项，`(task,employee)`过滤唯一；无技师账单允许待分配后认领 |
| `vis_visit_record` | `task_id/participant_id/employee_id bigint`, `result_code`, `satisfaction_score tinyint`, `complaint_flag bit`, `content nvarchar(2000)`, `next_follow_at`, 审计字段 | 已落地；append-only；已联系必须评分，未接通/继续跟进必须有下次时间 |
| `vis_feedback` | `feedback_no`, `visit_task_id/visit_record_id/member_id/bill_id/store_id`, `channel`, `score nullable`, `content`, `complaint_type`, `status`, `handler_id`, `handle_result`, `handled_at/resolved_at/closed_at`, `due_hours int`, `due_at datetime2(3)`, 审计字段和`row_version` | 已落地；回访客诉记录唯一建单；未评分保持为空；时限按建单/重开参数保存快照，待处理/处理中实时派生超时；四态状态机 |
| `vis_feedback_action` | `feedback_id`, `action_type`, `from_status/to_status`, `handler_id`, `content`, `created_at/by` | 已落地；创建、分配、备注、解决、关闭、重开均append-only |
| `vis_satisfaction_rule` | `rule_name nvarchar(100)`, `keyword_pattern nvarchar(500)`, `score tinyint`, `component_mapping_json nvarchar(max)`, `priority int`, `status`, 审计字段和`row_version` | 已落地；名称唯一、评分1~5、映射必须为JSON；按启用状态、优先级、id执行字面关键词匹配 |
| `mkt_sms_task` | `task_no varchar(32)`, `task_name nvarchar(200)`, `audience_type varchar(32)`, `segment_id bigint`, `content_ciphertext nvarchar(max)`, `template_id bigint`, `scheduled_at/started_at/finished_at datetime2`, `total/success/failure int`, `provider_code varchar(64)` | 短信批次；内容受字段权限 |
| `mkt_sms_message` | `task_id bigint`, `mobile_ciphertext/hash/last4`, `provider_message_id varchar(128)`, `send_status/delivery_status varchar(32)`, `failure_code/message`, `sent_at/delivered_at datetime2` | provider_message_id 索引 |
| `mkt_sms_inbound` | `provider_message_id varchar(128)`, `mobile_ciphertext/hash/last4`, `content_ciphertext nvarchar(max)`, `received_at datetime2`, `matched_member_id bigint` | 回调去重；内容加密 |

## 9. 通知、提成、工资、目标和报表表

| 表名 | 主要字段（类型） | 约束和用途 |
| --- | --- | --- |
| `ntf_template` | `event_code varchar(64)`, `channel varchar(32)`, `template_name nvarchar(100)`, `title_template/body_template nvarchar(max)`, `variables_json nvarchar(max)` | `(event_code,channel)` 有效版本唯一 |
| `ntf_notification` | `notification_no varchar(32)`, `event_code varchar(64)`, `recipient_user/member_id bigint`, `channel varchar(32)`, `title/body nvarchar(max)`, `business_type/id`, `sent_at/read_at datetime2`, `failure_reason nvarchar(1000)` | 站内消息及扩展渠道发送记录 |
| `ntf_announcement` | `title nvarchar(200)`, `content nvarchar(max)`, `scope_type varchar(32)`, `valid_from/to datetime2`, `publisher_id bigint`, `published_at datetime2` | 门店范围存关联表 |
| `ntf_announcement_store` | `announcement_id bigint`, `store_id bigint` | 联合唯一 |
| `comm_plan` | `plan_code/name`, `scene`, `calculation_mode`, `rate/fixed_amount`, `store_id/position_id`, `effective_from/to`, `status`, `rule_version`, `row_version` | 已落地；当前方案，按场景、门店、职务、日期匹配 |
| `comm_plan_revision` | `plan_id`, `rule_version`, 方案规则完整快照, `recorded_at/by` | 已落地；`(plan,version)`唯一，方案修改不丢历史 |
| `comm_plan_assignment` | `plan_id bigint`, `store_id bigint`, `position_id bigint`, `employee_id bigint`, `priority int`, `effective_from/to date` | 后续扩展到员工级、门店等级和复杂组合时使用 |
| `comm_ledger` | `ledger_no`, `employee_id/store_id`, `commission_type`, `source_type/id/no/line`, `base_amount/rate/commission_amount`, `calculation_status`, 方案快照, `formula_snapshot`, `correlation_id`, `reversed_ledger_id` | 已落地；append-only，唯一关联号防重，负向冲回可追溯 |
| `comm_third_party` | `party_code varchar(64)`, `party_name nvarchar(200)`, `contact_info_ciphertext nvarchar(1000)` | 第三方主数据 |
| `comm_share_rule` | `third_party_id bigint`, `service_id bigint`, `share_rate decimal(9,6)`, `effective_from/to date`, `rule_json nvarchar(max)` | 项目分润规则 |
| `comm_share_ledger` | `third_party_id bigint`, `bill_id/bill_line_id bigint`, `transaction_amount/share_amount decimal(19,4)`, `settlement_id bigint`, `occurred_at datetime2`, `reversed_ledger_id bigint` | 第三方分润事实 |
| `comm_third_party_settlement` | `settlement_no varchar(32)`, `third_party_id bigint`, `period_start/end date`, `total_amount decimal(19,4)`, `confirmed_at/by`, `paid_at datetime2` | 分润结算单 |
| `comm_employee_target` | `employee_id/store_id bigint`, `target_type varchar(32)`, `period_type varchar(16)`, `period_start/end date`, `target_value decimal(19,4)`, `custom_definition nvarchar(500)` | 员工/门店目标；范围内唯一 |
| `comm_adjustment` | `adjustment_no varchar(32)`, `adjustment_scope varchar(16)`, `employee_id/store_id bigint`, `period_start/end date`, `adjustment_type varchar(32)`, `amount decimal(19,4)`, `reason nvarchar(1000)`, `approval_status varchar(32)`, `commission_ledger_id bigint` | 技师/店总调账统一表 |
| `payroll_run` | `run_no varchar(32)`, `period_start/end date`, `store_id bigint`, `rule_cutoff_at datetime2`, `started_at/finished_at datetime2`, `confirmed_at/by`, `error_count int` | 工资计算批次 |
| `payroll_employee_result` | `run_id bigint`, `employee_id bigint`, `base_salary/service_commission/sales_commission/adjustment/other_amount/net_amount decimal(19,4)`, `calculation_json nvarchar(max)` | `(run,employee)` 唯一 |
| `payroll_result_line` | `employee_result_id bigint`, `line_type varchar(32)`, `source_type/source_id bigint`, `amount decimal(19,4)`, `description nvarchar(500)` | 工资项及来源钻取 |
| `rpt_daily_store_snapshot` | `business_date date`, `store_id bigint`, `metric_version varchar(32)`, `revenue/traffic/new_member/refund_amount decimal(19,4)`, `transaction_count int`, `generated_at datetime2` | 可重建日报快照；`(date,store,version)` 唯一 |
| `rpt_metric_definition` | `metric_code varchar(64)`, `metric_name nvarchar(100)`, `formula_description nvarchar(1000)`, `query_version varchar(32)`, `unit varchar(32)` | 统计口径版本和说明 |

`API-COMM-003`提成模拟测算只读`comm_plan`和员工/门店基础资料，不新建测算结果表，不写`comm_ledger`。`V202607291240`仅增加`iam_menu`的薪资测算入口。

回访基础表由`V202607291250`建立。结算、任务、参与人和首次状态在同一事务内提交；重复结算依靠`vis_visit_task.bill_id`唯一约束和应用幂等双重防重。任务只保存`PENDING/COMPLETED/CANCELLED`，`OVERDUE`是查询时根据到期时间派生的展示状态，不写入数据库。

服务反馈表由`V202607291260`建立，`V202607291310`补处理时限。`vis_feedback.visit_record_id`唯一，确保重复请求不会产生多张客诉单；当前只接收回访渠道且由人工明确标记客诉后创建。当前处理结果保存在主表方便查询，所有历史动作同时追加到`vis_feedback_action`，主表更新不得删除历史动作。`due_hours/due_at`保存建单或重开时采用的时限快照；`overdue`仅在查询时由未完成状态和当前时间派生，不写回状态字段。

## 10. 到家服务、AI 和第三方集成表

| 表名 | 主要字段（类型） | 约束和用途 |
| --- | --- | --- |
| `home_address` | `member_id bigint`, `contact_name nvarchar(100)`, `mobile_ciphertext/hash/last4`, `city/district/address`, `longitude/latitude decimal(10,7)`, `is_default bit` | 顾客服务地址；敏感字段加密 |
| `home_order` | `order_no varchar(32)`, `member_id bigint`, `store_id bigint`, `service_area_id bigint`, `address_snapshot_json nvarchar(max)`, `appointment_start/end datetime2`, `service_amount/visit_fee/discount_amount/payable_amount decimal(19,4)`, `assigned_employee_id bigint`, `accepted_at/dispatched_at/arrived_at/started_at/completed_at/cancelled_at`, `bill_id bigint`, `idempotency_key varchar(128)` | 到家订单状态机；编号唯一 |
| `home_order_line` | `home_order_id bigint`, `service_id bigint`, `service_name_snapshot nvarchar(200)`, `quantity decimal(19,4)`, `unit_price/amount decimal(19,4)` | 到家服务明细 |
| `home_order_status_history` | `home_order_id bigint`, `from_status/to_status varchar(32)`, `operator_type/id`, `longitude/latitude decimal(10,7)`, `note nvarchar(500)`, `occurred_at datetime2` | append-only 状态时间线 |
| `ai_task` | `task_no varchar(32)`, `scene varchar(64)`, `subject_type/id`, `requester_id bigint`, `model_code/version varchar(128)`, `input_hash char(64)`, `input_summary_json nvarchar(max)`, `started_at/finished_at`, `latency_ms int`, `token_usage_json nvarchar(max)`, `error_code/message` | 不保存未脱敏业务原文 |
| `ai_result` | `task_id bigint`, `result_type varchar(64)`, `result_json nvarchar(max)`, `confidence decimal(5,4)`, `evidence_json nvarchar(max)`, `expires_at datetime2` | 结构化、可解释结果 |
| `ai_feedback` | `result_id bigint`, `user_id bigint`, `feedback_type varchar(16)`, `edited_result_json nvarchar(max)`, `comment nvarchar(1000)`, `created_at datetime2` | 采用/驳回/修改反馈 |
| `ai_configuration` | `config_key varchar(128)`, `value_ciphertext nvarchar(max)`, `is_secret bit`, `effective_from datetime2`, `changed_by bigint` | 大模型 Key 加密且不可回显 |
| `intg_provider_config` | `provider_type varchar(32)`, `provider_code varchar(64)`, `store_id bigint`, `config_ciphertext nvarchar(max)`, `enabled bit`, `last_health_at datetime2`, `health_status varchar(32)` | 支付/短信/地图/OSS 配置 |
| `intg_callback_event` | `provider_type/code`, `external_event_id varchar(128)`, `event_type varchar(64)`, `signature_valid bit`, `payload_hash char(64)`, `payload_ciphertext nvarchar(max)`, `received_at/processed_at datetime2`, `process_status varchar(32)`, `retry_count int`, `error_message nvarchar(1000)` | `(provider,event_id)` 唯一；回调幂等 |
| `intg_outbox_event` | `event_id varchar(64)`, `aggregate_type/id`, `event_type varchar(64)`, `payload_json nvarchar(max)`, `occurred_at/published_at datetime2`, `retry_count int`, `last_error nvarchar(1000)` | 本地事务消息；短信/通知/报表异步触发 |

## 11. 历史数据迁移表

迁移记录与 Flyway 结构版本不是一回事。Flyway 管“数据库结构如何变化”，以下表管“旧系统业务数据何时、以何规则迁到新库”。

| 表名 | 字段 | 用途 |
| --- | --- | --- |
| `migration_batch` | `id`, `batch_no varchar(32)`, `batch_type varchar(16)`, `source_backup_id nvarchar(255)`, `source_cutoff_at datetime2`, `started_at/finished_at`, `status`, `initiated_by`, `code_version varchar(64)`, `schema_version varchar(64)`, `note` | 演练/正式迁移批次；batch_no 唯一 |
| `migration_step` | `id`, `batch_id`, `step_code`, `source_table`, `target_table`, `started_at/finished_at`, `read_count/insert_count/update_count/skip_count/error_count`, `status`, `checksum`, `note` | 每张表/每个步骤进度 |
| `legacy_id_map` | `id`, `batch_id`, `entity_type`, `legacy_table`, `legacy_id nvarchar(128)`, `new_table`, `new_id bigint`, `legacy_business_no nvarchar(128)`, `mapped_at` | `(entity_type,legacy_table,legacy_id)` 唯一 |
| `migration_error` | `id`, `batch_id`, `step_id`, `legacy_table`, `legacy_id`, `error_code`, `error_message`, `source_snapshot_json`, `resolution_status`, `resolved_by/at`, `resolution_note` | 逐行错误和人工处理证据 |
| `migration_reconciliation` | `id`, `batch_id`, `metric_code`, `scope_type/id`, `source_value/target_value/difference decimal(38,4)`, `tolerance decimal(38,4)`, `result`, `evidence_file_id`, `checked_at/by`, `note` | 数量、金额、余额、积分、次数对账 |

首批必须对账：会员总数/有效数、会员卡数、储值余额汇总、积分汇总、次卡剩余次数和价值、账单数及金额、各支付方式金额、未完成预约、库存、提成和工资结果。金额与资产差异不得以“人工改库”关闭，必须记录规则、修复脚本和复核结果。

## 12. 旧表到新表的首轮映射

| 旧系统表/模块 | 新表 | 迁移注意事项 |
| --- | --- | --- |
| `cst_customers` | `mem_member` | 合并散落属性；`text_attribute*` 必须逐项确认，不盲迁 |
| `cst_membership_cards` | `mem_membership_card` | 卡号、注册/撤销历史、换卡引用 |
| `cst_mc_accounts` | `ast_balance_account`、`ast_point_account` | 旧 `Double` 金额需核对单位、精度和负数 |
| `cst_mc_*_transations` | `ast_balance_ledger`、`ast_point_ledger` | 拼写和类型众多，先按来源分类再生成统一流水 |
| `cst_mc_item_suite_accounts`、`*_balances` | `ast_member_card`、`ast_member_card_balance` | 次卡总次、余次、项目组、到期日逐卡对账 |
| `cst_vouchers`、`cst_voucher_codes` | `cat_voucher`、`ben_voucher_code`、`ben_voucher_ledger` | 定义与实例拆分，核对绑定/核销状态和历史规则快照 |
| `item_service_codes`、生成任务 | `ben_service_code*` | 旧码迁移后只保存安全展示和哈希 |
| `oe_reserve_bill_*` | `apt_appointment*` | 映射预约状态、项目、员工、门店及转账单关系 |
| `oe_bill_headers/lines` | `trd_bill`、`trd_bill_line`、`trd_bill_line_employee` | 旧表公式字段不迁；名称、价格保存快照 |
| `oe_bill_*payment*` | `trd_payment`、资产流水 | 防止同一支付同时算现金与储值 |
| `oe_bill_*refund*` | `trd_reversal`、`trd_payment_refund` | 区分作废、冲销、退款和回滚 |
| `hr_organization_units` | `org_organization`、`org_store` | 还原总部/区域/门店层级 |
| `hr_employees` | `org_employee` | 与系统用户、职务、主店分离 |
| `hr_employee_*comission*` | `comm_plan`、`comm_ledger` | 保留旧公式快照；处理拼写和跨月负向记录 |
| `items/item_suites/item_groups` | `cat_product/service/card_type/category` | 依据 item_type 分类，不能只按表名迁 |
| `inv_*` | `inv_stock/ledger/transfer/count` | 用流水重算现存量并与旧汇总比对 |
| `sec_users/roles/privileges/menu` | `iam_*` | 密码不能直接沿用弱算法；首次登录重置 |
| `sec_action_logs` | `sys_audit_log` 或历史归档 | 旧日志只读归档，字段脱敏 |
| `message_mg*` | `mkt_sms_*` | 内容和手机号加密，通道状态重新映射 |

该映射必须在取得完整数据库备份后用真实 DDL、行数和样例更新。当前旧源码压缩包不完整，Hibernate 映射只能确认结构线索，不能代替真实数据库字典。

## 13. 索引、约束和数据量策略

### 13.1 必建索引

- 唯一：所有业务编号、用户账号、组织/员工/商品代码、卡号、服务码哈希、第三方事件 id、幂等键。
- 会员：`mobile_hash`、`mobile_last4`、`owner_store_id + status`、`last_visit_at`。
- 预约：`store_id + start_at + status`、`employee_id + start_at/end_at`、`member_id + start_at`。
- 账单：`store_id + settled_at + status`、`member_id + settled_at`、`bill_no`、`appointment_id`。
- 流水：`account/card/employee + occurred_at`、`source_type + source_id`、`correlation_id`。
- 报表：支付、账单行、员工分配和提成按业务日期/门店建立覆盖索引，依据执行计划调整。

### 13.2 约束

- 所有外键明确 `NO ACTION`，禁止级联删除业务历史。
- 金额、次数、比例按业务加 `check`；退款/冲销等负向业务用交易类型表达，不允许任意负数绕过规则。
- JSON 字段使用 `check (ISJSON(field)=1)`；状态字段使用应用枚举和检查约束双重控制。
- 对高并发余额/扣次使用账户行锁或乐观锁，更新账户与写流水必须在同一事务。

### 13.3 归档和保留

首版不做物理分表。`sys_audit_log`、`intg_callback_event`、短信明细和各类流水按月评估数据量；超过单表 5000 万行或明显影响 SLA 后再通过独立 Migration 引入分区。财务、资产、提成和迁移对账记录不得随普通日志清理。

## 14. Flyway Migration 规则

实际 SQL 放在：

```text
backend/src/main/resources/db/migration/
├── V202607290900__init_common.sql
├── V202607290910__init_iam_org.sql
├── V202607290920__init_catalog.sql
├── V202607290930__init_member_asset.sql
├── V202607290940__init_appointment_trade.sql
├── V202607290950__init_inventory_benefit.sql
├── V202607291000__init_visit_marketing_notification.sql
├── V202607291010__init_commission_payroll.sql
├── V202607291020__init_home_ai_integration.sql
├── V202607291030__init_migration_audit.sql
└── R__reporting_views.sql
```

规则：

1. 命名为 `VyyyyMMddHHmm__英文小写说明.sql`；时间版本全项目唯一。
2. 已在任何共享环境执行的版本文件禁止修改、改名或删除；修复只能新增更高版本。
3. `R__` 只放可重复创建的视图、函数或存储过程，不放业务表和不可逆数据修复。
4. 每个 Migration 文件顶部写需求号、变更原因、影响表、回滚/恢复方式、数据验证 SQL。
5. DDL 与数据回填分开；大表变更先演练执行时长、锁等待和磁盘增长。
6. 开发启动时执行 `flyway validate`、`flyway migrate`；CI 必须用空库从 V1 完整迁移。
7. `dbo.flyway_schema_history` 自动记录版本、描述、校验和、执行人、耗时和结果，禁止手工修改。
8. 共享开发、测试、预生产和生产禁止手工执行未归档 SQL；紧急 SQL 也要先补 Migration 和审批记录。
9. 非本地环境禁用 `flyway clean`；上线前完成全库备份和恢复演练。

每次变更的人工说明写入 `plan/migration/`，详见其中 README；说明文件与实际 SQL 版本同号，记录功能/API/页面影响、验证和部署结果。

## 15. 首版数据库完成标准

- 空库可一次执行全部 Migration，重复启动不会重复建表或重复写基础数据。
- 核心外键、唯一键、检查约束和索引已建立；API 字段与表字段能够逐项对应。
- 会员→预约→账单→结算→资产扣减→提成→回访闭环可在同一测试库运行。
- 调账、冲销、退卡和换卡均生成正反流水，原始事实不被覆盖。
- 使用旧库样例完成至少一次迁移，并对会员、储值、积分、次卡、账单和支付做数量与金额核对。
- 数据库账号按应用读写、迁移 DDL、只读报表、运维备份分权；配置中无明文生产密码。
